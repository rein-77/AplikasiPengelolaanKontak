CREATE TABLE IF NOT EXISTS kontak (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    nama TEXT NOT NULL,
    no_hp TEXT NOT NULL,
    kategori TEXT CHECK(kategori IN ('Keluarga', 'Teman', 'Kerjaan')) NOT NULL
);